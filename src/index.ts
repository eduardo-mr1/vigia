export { analyzeFiles, analyzeSource, collectTestFiles, isTestFile } from './analyze';
export { countBySeverity, renderReport } from './report';
export { rules } from './rules';
export type { AnalysisResult, Finding, Severity } from './types';
